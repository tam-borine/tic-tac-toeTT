require 'game'

describe Game do
  it "players can take turns to mark grid field"

  it "a turn ends when a player marks a grid field"

  it "a player wins if marked all fields in a row, column or diagonal"

  it "game ends when a player wins"

  it "game ends when all fields are taken"

  it "player can claim a field on grid if not taken"
  it "is easy to hook up to a UI"
end
